{
  "name": "token",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "setup": "node setup.js",
    "clean": "rm -rf ~/.config && sudo truffle compile && sudo chown -R $USER:$(id -gn $USER) ~/.config && sudo chown -R $USER:$(id -gn $USER) ./"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bluebird": "^3.5.3",
    "bn.js": "^4.11.8",
    "openzeppelin-solidity": "^2.1.1",
    "promise-retry": "^1.1.1",
    "readline": "^1.3.0",
    "readline-promise": "^1.0.3",
    "truffle-hdwallet-provider": "^1.0.2"
  }
}
